import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className="f3 link black underline pa3 pointer">登出</p>
        </nav>
    );
}

export default Navigation;